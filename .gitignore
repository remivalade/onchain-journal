# Dependency directories
# These folders can be regenerated by running 'npm install' or 'yarn install'
node_modules/

# Build and cache artifacts
# These are generated when you compile your contracts with tools like Hardhat or Foundry
artifacts/
cache/
out/
build/

# Log files
# These files are for debugging and should not be part of the repository
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
pnpm-debug.log*

# Environment variables
# This is CRITICAL. Never commit your private keys, API keys, or other secrets.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Operating System generated files
# These are metadata files from your OS and are not part of the project code.

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
ehthumbs_vista.db
*.stackdump

# IDE and editor directories
# These are user-specific settings for your code editor.
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Coverage reports
# Generated when you run test coverage analysis
coverage/
coverage.json
